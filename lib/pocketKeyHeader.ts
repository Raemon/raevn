// Its own file so the phone editor (a client component) can name the header
// without pulling lib/pocketAccess — and node:crypto with it — into the bundle.
export const POCKET_KEY_HEADER = 'x-pocket-key';
