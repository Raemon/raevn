// Your own card and each family member's card are cut the same way, so the
// RSVP box reads as a stack of one form per person rather than one long form.
export const PANEL_CARD_CLASS_NAME =
  'flex w-full flex-col items-center gap-4 rounded-md border border-white/50 bg-white/10 p-4';
