// Shared by the form and the API route so client and server caps cannot drift.

export const MAX_FAMILY_MEMBERS = 10;
export const MAX_GUEST_NAME_LENGTH = 120;
export const MAX_GUEST_NOTE_LENGTH = 2000;
